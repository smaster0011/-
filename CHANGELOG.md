# Changelog

## v1.0.0

首个公开分享版本。

### 功能

- 提供半透明黑色遮挡条，可用于遮挡屏幕字幕，辅助语言学习或演示遮罩。
- 支持窗口置顶、无边框显示。
- 支持鼠标拖动移动窗口。
- 支持右下角拖动缩放窗口。
- 支持鼠标悬浮显示提示文字和缩放手柄。
- 支持锁定/解锁，锁定后禁止移动和缩放。
- 支持透明度调节：100%、90%、75%、60%、45%。
- 支持重置到底部居中和恢复默认设置。
- 支持退出时保存本地配置，并在下次启动时恢复。
- 支持配置损坏、字段缺失、窗口越界时自动回退默认设置。
- 支持 Windows 单实例运行，避免重复启动多个遮挡条。

### 隐私与限制

- v1.0.0 不联网、不采集数据、不读取浏览器内容、不识别屏幕内容、不保存观看记录。
- 软件不提供、不下载、不传播任何影视资源。
- 某些播放器独占全屏时可能无法遮挡，可改用窗口化全屏或浏览器播放。

### 技术

- 使用 Python 标准库 tkinter，无第三方 GUI 依赖。
- 提供无命令行黑框的 VBS 启动脚本。
